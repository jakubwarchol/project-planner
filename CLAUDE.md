# Project notes for Claude

## Git workflow

- **No branches in this repo.** All work happens directly on `main` and is
  pushed to `origin/main`. Do not create feature branches, even for larger
  changes — commit in coherent, reviewable chunks on `main` instead.
